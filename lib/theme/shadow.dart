import 'package:flutter/rendering.dart';

import 'theme.dart';

const topShadow = BoxShadow(
  color: shadowColor,
  offset: Offset(4, 4),
  blurRadius: 4,
  spreadRadius: .5,
);

const bottomShadow = BoxShadow(
  color: shadowColor,
  offset: Offset(-2, -2),
  blurRadius: 4,
  spreadRadius: .5,
);

